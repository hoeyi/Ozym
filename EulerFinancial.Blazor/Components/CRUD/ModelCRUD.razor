@typeparam TModel

@inherits ModelDetail<TModel>

<PageTitle>@PageTitle</PageTitle>

@code{
    /// <summary>
    /// Gets or sets the <see cref="IController{TModel}"/> for this component.
    /// </summary>
    [Inject]
    protected IController<TModel> Controller { get; set; }
}