@inherits ModelCRUD<InvestmentModel>

@if(IsLoading)
{
    <LoadingSvg />
}
else
{
    <ModelForm TModel="InvestmentModel">
        <ModelFormLineItem MemberName="@nameof(InvestmentModel.DisplayName)"
            ValidationMessageDelegate="@(() => ViewModel.DisplayName)">
            @if(ReadOnly)
            {
                <div class="form-control">
                    @ViewModel.DisplayName
                </div>
            }
            else
            {
                <InputText class="form-control" @bind-Value=ViewModel.DisplayName
                    disable="@Disabled"
                    DisplayName=@(NameFor(x => x.DisplayName)) />
            }
        </ModelFormLineItem>
        <ModelFormLineItem MemberName="@nameof(InvestmentModel.Notes)"
            ValidationMessageDelegate="@(() => ViewModel.Notes)">
            @if(ReadOnly)
            {
                <div class="form-control text-area">
                    @ViewModel.Notes
                </div>
            }
            else
            {
                <InputTextArea class="form-control" @bind-Value=ViewModel.Notes
                    disable="@Disabled"
                    DisplayName=@(NameFor(x => x.Notes)) />
            }
        </ModelFormLineItem>
    </ModelForm>
}