@inherits ModelCRUD<AccountCustodian>

@if (ViewModel is null)
{
    <LoadingSvg />
}
else
{
    <ModelForm TViewModel="AccountCustodian" >
        <ModelFormLineItem ForMember="@(() => ViewModel.CustodianCode)">
            @if(ReadOnly)
            {
                @ViewModel.CustodianCode
            }
            else
            {
                <InputText class="form-control" @bind-Value=ViewModel.CustodianCode
                    disable="@Disabled"
                    DisplayName=@(NameFor(x => x.CustodianCode)) />
            }
        </ModelFormLineItem>
        <ModelFormLineItem ForMember="@(() => ViewModel.DisplayName)">
            @if(ReadOnly)
            {
                @ViewModel.DisplayName
            }
            else
            {
                <InputText class="form-control" @bind-Value=ViewModel.DisplayName
                    disable="@Disabled"
                    DisplayName=@(NameFor(x => x.DisplayName)) />
            }
        </ModelFormLineItem>
    </ModelForm>
}