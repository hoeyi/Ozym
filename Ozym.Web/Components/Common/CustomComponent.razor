@inherits ComponentBase

@code{
        /// <summary>
        /// Gets or sets whether the user interface should present a loading state.
        /// </summary>
        protected bool IsLoading { get; set; } = false;
}