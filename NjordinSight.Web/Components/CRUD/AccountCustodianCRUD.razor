@inherits ModelCRUD<AccountCustodian>

@if (ModelDto is null)
{
    <LoadingSvg />
}
else
{
    <ModelForm TModelDto="AccountCustodian" >
        <ModelFormLineItem ForMember="@(() => ModelDto.CustodianCode)">
            @if(ReadOnly)
            {
                @ModelDto.CustodianCode
            }
            else
            {
                <InputText class="form-control" @bind-Value=ModelDto.CustodianCode
                    disable="@Disabled"
                    DisplayName=@(NameFor(x => x.CustodianCode)) />
            }
        </ModelFormLineItem>
        <ModelFormLineItem ForMember="@(() => ModelDto.DisplayName)">
            @if(ReadOnly)
            {
                @ModelDto.DisplayName
            }
            else
            {
                <InputText class="form-control" @bind-Value=ModelDto.DisplayName
                    disable="@Disabled"
                    DisplayName=@(NameFor(x => x.DisplayName)) />
            }
        </ModelFormLineItem>
    </ModelForm>
}