@typeparam TModelDto
@attribute [CascadingTypeParameter(nameof(TModelDto))]

@inherits ModelComponent<TModelDto>

@code {
    /// <summary>
    /// Gets or sets the <see cref="TModelDto"/> for this component.
    /// </summary>
    //[CascadingParameter(Name = nameof(ViewModel))]
    [CascadingParameter]
    public TModelDto ModelDto { get; set; } = default!;

    /// <summary>
    /// Gets or sets whether the component is disabled.
    /// </summary>
    [Parameter]
    public bool Disabled { get; set; }

    /// <summary>
    /// Gets or sets whether the component is rendered as readonly.
    /// </summary>
    [Parameter]
    public bool ReadOnly { get; set; } = true;
}
    